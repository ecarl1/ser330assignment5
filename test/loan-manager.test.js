const LoanManager = require('../src/examples/loan-manager.js')

describe('canGetLoan', () => {
  it('should return true for someone with a lower income, no criminal record, the required docs and no existing loan', () => {


    // const income = 30000
    // const criminalRecord = false
    // const hasRequiredDocs = true
    // const hasLoan = false

    // const loanManager = new LoanManager()
    // const result = loanManager.canGetLoan(income, criminalRecord, hasRequiredDocs, hasLoan)
    // expect(result).toBe(true)
  })
})
